# project-center-nuxt

> CIIP——项目中心

## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:3033
npm run dev

# build for production with minification
npm run build

```
